# Graph MCP Gateway (custom Microsoft 365 gateway with guardrails)
GRAPH_MCP_CLIENT_ID=
GRAPH_MCP_CLIENT_SECRET=
GRAPH_MCP_TENANT_ID=

# Optional split deployment override (Graph MCP Gateway (split deployment))
GRAPH_MCP_URL=http://host.docker.internal:18790/mcp
